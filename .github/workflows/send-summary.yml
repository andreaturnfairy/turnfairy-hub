name: Send Post-Call Summary (DISABLED)
# Disabled — email automation moved to Netlify scheduled functions
# Trigger post-call summary manually via:
# https://turnfairy-hub.netlify.app/.netlify/functions/saturday-briefing
on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type ENABLE to re-enable this workflow'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is disabled
        run: |
          echo "Email automation has moved to Netlify scheduled functions."
          exit 0
