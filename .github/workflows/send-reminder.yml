name: Send Pre-Call Reminder (DISABLED)
# Disabled — email automation moved to Netlify scheduled functions (saturday-briefing.js)
# Uses Resend API via Netlify. GitHub Actions version used Azure Graph API which requires
# Exchange Online mailbox — not available on current Microsoft 365 Family plan.
on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type ENABLE to re-enable this workflow'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is disabled
        run: |
          echo "Email automation has moved to Netlify scheduled functions."
          echo "See turnfairy-hub.netlify.app/.netlify/functions/saturday-briefing"
          exit 0
